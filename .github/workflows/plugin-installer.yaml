name: Plugin Installer CI

on:
  push:
    branches:
      - main
    paths:
      - 'plugin-installer/**'
      - '.github/workflows/plugin-installer.yaml'
  pull_request:
    branches:
      - main
    paths:
      - 'plugin-installer/**'
      - '.github/workflows/plugin-installer.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ${{ vars.REGISTRY }}

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install dependencies
        run: |
          sudo apt-get -y update
          sudo apt-get -y install skopeo

      - name: Run tests
        run: RUN_INTEGRATION_TESTS=true ./plugin-installer/install_plugins_test.sh

  build:
    name: Build image
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Build image
        run: |
          sudo apt-get -y update
          sudo apt-get -y install podman

          CONTAINER_TOOL=podman make install-dp-build

      - name: Login to registry (${{ env.REGISTRY }})
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ vars.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Push image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: CONTAINER_TOOL=podman make install-dp-push
