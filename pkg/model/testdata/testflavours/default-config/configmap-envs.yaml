apiVersion: v1
kind: ConfigMap
metadata:
  name: base-configmap-envs
data:
  BASE_ENV1: "base value 1"
  BASE_ENV2: "base value 2"
  BACKSTAGE_NAME: "{{.Backstage.Name}}"
  SERVICE_URL: "https://{{.Backstage.Name}}.{{.Backstage.Namespace}}.svc"
