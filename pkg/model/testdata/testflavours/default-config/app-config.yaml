apiVersion: v1
kind: ConfigMap
metadata:
  name: base-app-config
data:
  base.app-config.yaml: |
    app:
      title: Base Backstage
    backend:
      database:
        connection:
          password: ${POSTGRES_PASSWORD}
