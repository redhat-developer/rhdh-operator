apiVersion: v1
kind: ConfigMap
metadata:
  name: flavor1-app-config
data:
  flavor1.app-config.yaml: |
    app:
      title: Flavor1 Backstage
    flavor1:
      enabled: true
      apiKey: ${FLAVOR1_API_KEY}
