apiVersion: v1
kind: ConfigMap
metadata:
  name: base-dynamic-plugins
data:
  "dynamic-plugins.yaml": |
    includes:
      - dynamic-plugins.default.yaml
    plugins:
      - package: "./plugin-base"
        enabled: true
