apiVersion: v1
kind: ConfigMap
metadata:
  name: default-dynamic-plugins
data:
  "dynamic-plugins.yaml": |
    includes:
      - dynamic-plugins.custom.yaml
    plugins:
      - package: "plugin-b"
        enabled: true
        integrity: "sha512-overlay"
      - package: "plugin-c"
        enabled: true
