apiVersion: v1
kind: ConfigMap
metadata:
  name: default-dynamic-plugins
data:
  dynamic-plugins.yaml: |
    plugins:
      - disabled: false
        package: "plugin"
        dependencies:
          - ref: plugin
      - disabled: true
        package: "disabled"
        dependencies:
          - ref: disabled      
